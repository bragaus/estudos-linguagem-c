#include <stdio.h>

int main() {
    int cont = 1;

    printf("Contagem progressiva de 1 a 100:\n");
    while (cont <= 100) {
        printf("%d\n", cont);
        cont++;
    }

return 0;
}